#include <stdio.h>

int main()
{ /*
  //ARRAY USER INPUT
  int marks[10];
  for (int i = 0; i <= 3; i++)
  {
      scanf("%d", &marks[i]);  //%D ER POR KOKHONO SPACE DEOA JABENA
  }
*/

    // PRINTING ARRAY---
    int marks[5] = {55, 45, 54, 54};

    for (int i = 0; i < 4; i++)
    {
        printf("The marks is = %d\n", marks[3]);
    }

    return 0;
}